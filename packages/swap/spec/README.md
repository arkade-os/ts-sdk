# Swap SDK v2 specs — moved

The v2 planning docs that lived here moved to the cross-repo `meta` hub on
2026-08-28:

- Track: `meta/tracks/swap-sdk-v2/` (in the `ark/meta` repository)
- Start at `tracks/swap-sdk-v2/README.md`; the full API spec, RFC-lite,
  open-RFQ notes, roadmap, and deprecation map sit beside it.

Nothing else in this directory is authoritative; update the meta track, not
this file.
